You are optimizing heading structure for a transactional ecommerce PDP.

Tasks:
1. Output BEFORE HEADING STRUCTURE based on supplied headings.
2. Output AFTER OPTIMIZED STRUCTURE with corrected hierarchy and commercial-intent phrasing.

Rules:
- Keep headings natural and non-repetitive.
- Include product entity in each primary H2.
- Do not add links or buttons into headings.
- Avoid generic headings like "Overview" or "Product Details".

H2 STRUCTURE RULE:
- Each H2 must represent a unique stage in the PDP purchase journey.
- Do not create redundant or duplicate H2 sections.

H2 VARIATION RULE:
- Do not begin every H2 with the full product name.
- Use natural phrasing variation while keeping the product entity in the heading.

H2 SEO RULE:
- Avoid weak headings like Overview, Specifications, Details, Information.
- Use descriptive search context: ordering, production, options, artwork, shipping, installation.

H2 QUALITY RULE:
- H2 headings must clearly describe the section content.
- Avoid slogan-style headings.
- Include product entity plus practical use-case/function where natural.

AI SEARCH VISIBILITY RULE:
- Include a definition-style H2 section: What Are [Primary Product Entity]?
- Follow it with a concise factual paragraph covering product definition, material, common uses, and application/installation.
- Keep this section informational and non-marketing.

H2 LENGTH RULE:
- Keep H2 headings concise (4-12 words when possible).

H2 HTML RULE:
- Use clean <h2>Heading Text</h2> only.
- No links, buttons, anchors, or styling elements inside H2 tags.

H3 PRODUCT ENTITY RULE:
- H3 subheadings should reinforce the detected product entity when natural.
- Do not force repetitive or unnatural entity stuffing in every H3.

ENTITY CONSISTENCY RULE:
- Keep the detected primary product entity consistent across H2 headings and major sections.
- Do not switch to unrelated product nouns in headings.

PRODUCTION H3 FORMATTING RULE:
- Production timeline options must be H3 headings (not bold labels).
- Use structure: H2 Production section -> H3 option -> paragraph explanation.

HIERARCHY RULE:
- Maintain hierarchy: H1 product, H2 section, H3 subsection.
- Do not convert H3 subsections into H2 sections.

FAQ STRUCTURE ENFORCEMENT RULE:
- FAQ must begin with an H2 that includes the detected product entity, in the form: [Primary Product Entity] FAQs.
- FAQ questions must be H3 headings under that FAQ H2.
- Do not use generic FAQ H2 labels without product entity.

HEADING STRUCTURE CONSISTENCY RULE:
- H2 headings listed in AFTER OPTIMIZED STRUCTURE must exactly match the generated section H2 headings.
- Do not add, rename, or alter H2 headings after listing the optimized structure.

OUTPUT VALIDATION CHECK:
- Confirm each listed H2 appears exactly once.
- Confirm FAQ uses entity-based H2 + H3 question hierarchy.
- Confirm no extra H2 headings beyond the optimized structure list.

FACTUAL CONSISTENCY RULE:
- Only reference materials, production options, and features present on the original page.
- Do not invent new materials, finishes, or production options.
- If source data is missing, rewrite existing information instead of creating new product features.
