import { renderProcessSteps } from "./steps.js";
import { renderProcessTimeline } from "./timeline.js";

export const processVariants = {
  steps: renderProcessSteps,
  timeline: renderProcessTimeline
};
