import { renderTestimonialsGrid } from "./grid.js";
import { renderTestimonialsCarousel } from "./carousel.js";

export const testimonialsVariants = {
  grid: renderTestimonialsGrid,
  carousel: renderTestimonialsCarousel
};
